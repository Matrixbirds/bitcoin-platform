{
  "dependencies": {
    "bluebird": "^3.5.0",
    "co": "^4.6.0",
    "express": "^4.15.3",
    "mongoose": "^4.10.5",
    "morgan": "^1.8.2",
    "nconf": "^0.8.4",
    "nconf-yaml": "^1.0.2"
  },
  "devDependencies": {
    "chai": "^4.0.2",
    "co-mocha": "^1.2.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.4.2",
    "nyc": "^11.0.2",
    "supertest": "^3.0.0"
  },
  "scripts": {
    "test": "NODE_PATH=spec nyc --reporter=html --reporter=text mocha spec --recursive --opts .mocharc",
    "spec": "NODE_PATH=spec nyc --reporter=html --reporter=text mocha --opts .mocharc"
  }
}
